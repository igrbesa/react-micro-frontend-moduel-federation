/** Vitest-only stub for `cart/CartApp` remote (see shell/vite.config.ts resolve alias). */
export default function CartRemoteStub() {
  return <div data-testid="cart-remote-stub">cart-remote-stub</div>
}
